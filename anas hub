-- [[ ANAS HUB V21 - CLEAN & PURE NO ENCRYPTION ]] --
if game.CoreGui:FindFirstChild("AnasHubPremium") then
    game.CoreGui.AnasHubPremium:Destroy()
end

-- [[ 🔑 لیستا کلیلێن فەرمی یێن تە (Allowed Keys) ]] --
local AllowedKeys = {
    ["ANAS_VIP_999"] = true,
    ["ANAS_PREMIUM_5USD"] = true,
    ["KEY_2026_ANAS"] = true
}

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "AnasHubPremium"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game.CoreGui

-- ====== 🏢 ١. پەنجەرەیا کلیلێ (Key Verification Frame) ====== --
local KeyFrame = Instance.new("Frame")
KeyFrame.Size = UDim2.new(0, 320, 0, 180)
KeyFrame.Position = UDim2.new(0.35, 0, 0.35, 0)
KeyFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
KeyFrame.Active = true
KeyFrame.Draggable = true
KeyFrame.Parent = ScreenGui

local KeyCorner = Instance.new("UICorner")
KeyCorner.CornerRadius = UDim.new(0, 12)
KeyCorner.Parent = KeyFrame

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Size = UDim2.new(1, 0, 0, 40)
KeyTitle.Text = "🔑 ANAS HUB V21 - ENTER KEY"
KeyTitle.TextColor3 = Color3.fromRGB(0, 255, 255)
KeyTitle.BackgroundColor3 = Color3.fromRGB(25, 25, 40)
KeyTitle.Font = Enum.Font.GothamBold
KeyTitle.TextSize = 14
KeyTitle.Parent = KeyFrame

local TextBox = Instance.new("TextBox")
TextBox.Size = UDim2.new(0, 260, 0, 40)
TextBox.Position = UDim2.new(0, 30, 0, 60)
TextBox.PlaceholderText = "کلیلێ ل ڤێرە بنووسە..."
TextBox.Text = ""
TextBox.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.Font = Enum.Font.Gotham
TextBox.TextSize = 14
TextBox.Parent = KeyFrame

local SubmitBtn = Instance.new("TextButton")
SubmitBtn.Size = UDim2.new(0, 140, 0, 35)
SubmitBtn.Position = UDim2.new(0, 90, 0, 120)
SubmitBtn.Text = "🔒 Check Key"
SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
SubmitBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitBtn.Font = Enum.Font.GothamBold
SubmitBtn.TextSize = 14
SubmitBtn.Parent = KeyFrame

-- ====== 👑 ٢. مینیۆیا سەرەکى (Main Features Frame) ====== --
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 400, 0, 250)
MainFrame.Position = UDim2.new(0.3, 0, 0.3, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.Visible = false -- ل دەستپێکێ بەرزەیە هەتا کلیل ڕاست دەرکەبیت
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local MainTitle = Instance.new("TextLabel")
MainTitle.Size = UDim2.new(1, 0, 0, 45)
MainTitle.Text = "✨🔮 ANAS HUB V21 - MAIN MENU 🔮✨"
MainTitle.BackgroundColor3 = Color3.fromRGB(20, 20, 35)
MainTitle.TextColor3 = Color3.fromRGB(0, 255, 150)
MainTitle.Font = Enum.Font.GothamBold
MainTitle.TextSize = 15
MainTitle.Parent = MainFrame

-- دوگمەیا Auto Win Stage 12
local AutoWinBtn = Instance.new("TextButton")
AutoWinBtn.Size = UDim2.new(0, 340, 0, 45)
AutoWinBtn.Position = UDim2.new(0, 30, 0, 70)
AutoWinBtn.Text = "🏆 ACTIVATE AUTO WIN (STAGE 1-12)"
AutoWinBtn.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
AutoWinBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
AutoWinBtn.Font = Enum.Font.GothamBold
AutoWinBtn.TextSize = 14
AutoWinBtn.Parent = MainFrame

-- دوگمەیا WalkSpeed 60
local SpeedBtn = Instance.new("TextButton")
SpeedBtn.Size = UDim2.new(0, 160, 0, 45)
SpeedBtn.Position = UDim2.new(0, 30, 0, 135)
SpeedBtn.Text = "⚡ WalkSpeed (60)"
SpeedBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBtn.Font = Enum.Font.GothamBold
SpeedBtn.TextSize = 13
SpeedBtn.Parent = MainFrame

-- دوگمەیا JumpPower 120
local JumpBtn = Instance.new("TextButton")
JumpBtn.Size = UDim2.new(0, 160, 0, 45)
JumpBtn.Position = UDim2.new(0, 210, 0, 135)
JumpBtn.Text = "🚀 JumpPower (120)"
JumpBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
JumpBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpBtn.Font = Enum.Font.GothamBold
JumpBtn.TextSize = 13
JumpBtn.Parent = MainFrame

-- دوگمەیا داخستنێ
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 100, 0, 30)
CloseBtn.Position = UDim2.new(0, 150, 0, 200)
CloseBtn.Text = "❌ Close"
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.Gotham
CloseBtn.TextSize = 12
CloseBtn.Parent = MainFrame

-- ====== ⚙️ ٣. لۆجیک و فرمانێن کایێ (Functions) ====== --
SubmitBtn.MouseButton1Click:Connect(function()
    local input = TextBox.Text
    if AllowedKeys[input] then
        KeyFrame:Destroy() -- لادانا پەنجەرەیا کلیلێ
        MainFrame.Visible = true -- ڤەکرنا مینیۆیا سەرەکی
    else
        TextBox.Text = ""
        TextBox.PlaceholderText = "کلیل خەلەتە! (Wrong Key)"
    end
end)

SpeedBtn.MouseButton1Click:Connect(function()
    local p = game.Players.LocalPlayer
    if p.Character and p.Character:FindFirstChild("Humanoid") then
        p.Character.Humanoid.WalkSpeed = 60
    end
end)

JumpBtn.MouseButton1Click:Connect(function()
    local p = game.Players.LocalPlayer
    if p.Character and p.Character:FindFirstChild("Humanoid") then
        p.Character.Humanoid.JumpPower = 120
        p.Character.Humanoid.UseJumpPower = true
    end
end)

AutoWinBtn.MouseButton1Click:Connect(function()
    local player = game.Players.LocalPlayer
    task.spawn(function()
        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            for i = 1, 12 do
                for _, v in pairs(workspace:GetDescendants()) do
                    if v.Name == tostring(i) and v:IsA("BasePart") then
                        player.Character.HumanoidRootPart.CFrame = v.CFrame + Vector3.new(0, 3, 0)
                        task.wait(0.5)
                        break
                    end
                end
            end
        end
    end)
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)
    t2.TextSize = 15
    
    local b_win = Instance.new("TextButton", f2)
    b_win.Size = UDim2.new(0, 340, 0, 45)
    b_win.Position = UDim2.new(0, 30, 0, 70)
    b_win.Text = "🏆 ACTIVATE AUTO WIN (STAGE 1-12)"
    b_win.BackgroundColor3 = Color3.fromRGB(255, 170, 0)
    b_win.TextColor3 = Color3.fromRGB(0, 0, 0)
    b_win.Font = Enum.Font.GothamBold
    b_win.TextSize = 14
    
    local b_spd = Instance.new("TextButton", f2)
    b_spd.Size = UDim2.new(0, 160, 0, 45)
    b_spd.Position = UDim2.new(0, 30, 0, 135)
    b_spd.Text = "⚡ WalkSpeed (60)"
    b_spd.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    b_spd.TextColor3 = Color3.fromRGB(255, 255, 255)
    b_spd.Font = Enum.Font.GothamBold
    b_spd.TextSize = 13
    
    local b_jmp = Instance.new("TextButton", f2)
    b_jmp.Size = UDim2.new(0, 160, 0, 45)
    b_jmp.Position = UDim2.new(0, 210, 0, 135)
    b_jmp.Text = "🚀 JumpPower (120)"
    b_jmp.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
    b_jmp.TextColor3 = Color3.fromRGB(255, 255, 255)
    b_jmp.Font = Enum.Font.GothamBold
    b_jmp.TextSize = 13
    
    local b_cls = Instance.new("TextButton", f2)
    b_cls.Size = UDim2.new(0, 100, 0, 30)
    b_cls.Position = UDim2.new(0, 150, 0, 200)
    b_cls.Text = "❌ Close"
    b_cls.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
    b_cls.TextColor3 = Color3.fromRGB(255, 255, 255)
    b_cls.Font = Enum.Font.Gotham
    b_cls.TextSize = 12

    local keys = {["ANAS_VIP_999"]=true,["ANAS_PREMIUM_5USD"]=true,["KEY_2026_ANAS"]=true}
    sub.MouseButton1Click:Connect(function()
        if keys[box.Text] then f1:Destroy() f2.Visible = true else box.Text = "" box.PlaceholderText = "Wrong Key!" end
    end)
    b_spd.MouseButton1Click:Connect(function() pcall(function() game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 60 end) end)
    b_jmp.MouseButton1Click:Connect(function() pcall(function() game.Players.LocalPlayer.Character.Humanoid.JumpPower = 120 game.Players.LocalPlayer.Character.Humanoid.UseJumpPower = true end) end)
    b_win.MouseButton1Click:Connect(function()
        task.spawn(function()
            pcall(function()
                local hrp = game.Players.LocalPlayer.Character.HumanoidRootPart
                for i = 1, 12 do
                    for _, v in pairs(workspace:GetDescendants()) do
                        if v.Name == tostring(i) and v:IsA("BasePart") then
                            hrp.CFrame = v.CFrame + Vector3.new(0, 3, 0)
                            task.wait(0.5)
                            break
                        end
                    end
                end
            end)
        end)
    end)
    b_cls.MouseButton1Click:Connect(function() scr:Destroy() end)
end)
SpeedBtn.TextSize = 13
SpeedBtn.Parent = MainFrame

-- دوگمەیا JumpPower 120
local JumpBtn = Instance.new("TextButton")
JumpBtn.Size = UDim2.new(0, 160, 0, 45)
JumpBtn.Position = UDim2.new(0, 210, 0, 135)
JumpBtn.Text = "🚀 JumpPower (120)"
JumpBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
JumpBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
JumpBtn.Font = Enum.Font.GothamBold
JumpBtn.TextSize = 13
JumpBtn.Parent = MainFrame

-- دوگمەیا داخستنێ
local CloseBtn = Instance.new("TextButton")
CloseBtn.Size = UDim2.new(0, 100, 0, 30)
CloseBtn.Position = UDim2.new(0, 150, 0, 200)
CloseBtn.Text = "❌ Close"
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.Font = Enum.Font.Gotham
CloseBtn.TextSize = 12
CloseBtn.Parent = MainFrame

-- ====== ⚙️ ٣. لۆجیک و فرمانێن کایێ (Functions) ====== --
SubmitBtn.MouseButton1Click:Connect(function()
    local input = TextBox.Text
    if AllowedKeys[input] then
        KeyFrame:Destroy() -- لادانا پەنجەرەیا کلیلێ
        MainFrame.Visible = true -- ڤەکرنا مینیۆیا سەرەکی
    else
        TextBox.Text = ""
        TextBox.PlaceholderText = "کلیل خەلەتە! (Wrong Key)"
    end
end)

SpeedBtn.MouseButton1Click:Connect(function()
    local p = game.Players.LocalPlayer
    if p.Character and p.Character:FindFirstChild("Humanoid") then
        p.Character.Humanoid.WalkSpeed = 60
    end
end)

JumpBtn.MouseButton1Click:Connect(function()
    local p = game.Players.LocalPlayer
    if p.Character and p.Character:FindFirstChild("Humanoid") then
        p.Character.Humanoid.JumpPower = 120
        p.Character.Humanoid.UseJumpPower = true
    end
end)

AutoWinBtn.MouseButton1Click:Connect(function()
    local player = game.Players.LocalPlayer
    task.spawn(function()
        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            for i = 1, 12 do
                for _, v in pairs(workspace:GetDescendants()) do
                    if v.Name == tostring(i) and v:IsA("BasePart") then
                        player.Character.HumanoidRootPart.CFrame = v.CFrame + Vector3.new(0, 3, 0)
                        task.wait(0.5)
                        break
                    end
                end
            end
        end
    end)
end)

CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)
