-- [[ ANAS HUB V28.3 - THE MASTER EDITION ]] --
-- [[ STATUS: STABLE | IDENTITY: LOCKED | BYPASS: ACTIVE ]] --

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")

-- [ 🛡️ IDENTITY PROTECTION ] --
task.spawn(function()
    while task.wait(0.5) do
        local char = LocalPlayer.Character
        if char then
            for _, item in pairs(char:GetDescendants()) do
                if item.Name:lower():find("earring") then item:Destroy() end
            end
            if char:FindFirstChild("Animate") then
                char.Animate.idle.Animation1.AnimationId = "rbxassetid://507766388"
            end
        end
    end
end)

-- [ ⚙️ MAIN AUTOMATION ENGINE ] --
RunService.RenderStepped:Connect(function()
    local char = LocalPlayer.Character
    if not char then return end

    -- GHOST MODE (NOCLIP)
    for _, p in pairs(char:GetChildren()) do if p:IsA("BasePart") then p.CanCollide = false end end

    -- ESP & KILL & MAGNET & REPAIR
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            -- ESP
            local h = p.Character:FindFirstChild("AnasHubESP") or Instance.new("Highlight", p.Character)
            h.Name = "AnasHubESP"
            h.FillColor = (p.Character:FindFirstChild("Knife") or p.Character:FindFirstChildOfClass("Tool")) and Color3.fromRGB(255,0,0) or Color3.fromRGB(0,255,0)
            
            -- AUTO KILL
            if (p.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude < 15 then
                local tool = char:FindFirstChild("Knife") or char:FindFirstChildOfClass("Tool")
                if tool then tool:Activate() end
            end
        end
    end
    
    -- REPAIR & MAGNET
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("Model") and (obj.Name:find("Generator") or obj.Name:find("Repair")) then
            local prompt = obj:FindFirstChildWhichIsA("ProximityPrompt", true)
            if prompt then fireproximityprompt(prompt) end
        end
        if obj:IsA("Tool") and obj:FindFirstChild("Handle") then
            obj.Handle.CFrame = char.HumanoidRootPart.CFrame
        end
    end
end)

-- [ 🌍 SERVER HOPPER ] --
local function JoinIdealServer()
    local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?limit=100"
    local success, res = pcall(function() return game:HttpGet(url) end)
    if success then
        local data = HttpService:JSONDecode(res)
        for _, s in pairs(data.data) do
            if s.playing < 10 then TeleportService:TeleportToPlaceInstance(game.PlaceId, s.id, LocalPlayer) break end
        end
    end
end

-- [ GUI INTERFACE ] --
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Btn = Instance.new("TextButton", ScreenGui)
Btn.Size, Btn.Position = UDim2.new(0, 200, 0, 50), UDim2.new(0, 50, 0, 50)
Btn.Text, Btn.BackgroundColor3 = "🌍 JOIN IDEAL SERVER", Color3.fromRGB(50, 50, 200)
Btn.MouseButton1Click:Connect(JoinIdealServer)
