-- [[ ANAS HUB V27.5 - THE ULTIMATE FULL SCRIPT ]] --
-- [[ FEATURES: MAGNET, NOCLIP, AUTO-KILL, ESP, SERVER-JOIN, IDENTITY PROTECTION ]] --

if game:GetService("CoreGui"):FindFirstChild("AnasHubPremium") then
    game:GetService("CoreGui").AnasHubPremium:Destroy()
end

-- [ 1. IDENTITY PROTECTION (NO EARRINGS & HANDS IN SHORTS) ] --
task.spawn(function()
    while task.wait(1) do
        local char = game.Players.LocalPlayer.Character
        if char then
            for _, item in pairs(char:GetDescendants()) do
                if item.Name:lower():find("earring") then item:Destroy() end
            end
            if char:FindFirstChild("Animate") then
                char.Animate.idle.Animation1.AnimationId = "rbxassetid://507766388"
                char.Animate.idle.Animation2.AnimationId = "rbxassetid://507766388"
            end
        end
    end
end)

-- [ 2. GUI SETUP ] --
local ScreenGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size, MainFrame.Position, MainFrame.BackgroundColor3 = UDim2.new(0, 450, 0, 400), UDim2.new(0.3, 0, 0.25, 0), Color3.fromRGB(15, 15, 20)
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 14)

local function CreateButton(name, pos, func)
    local Btn = Instance.new("TextButton", MainFrame)
    Btn.Size, Btn.Position, Btn.Text = UDim2.new(0, 400, 0, 40), UDim2.new(0, 25, 0, pos), name
    Btn.MouseButton1Click:Connect(func)
    return Btn
end

-- [ 3. FUNCTIONS ] --
CreateButton("⚔️ LEGIT AUTO-KILL", 50, function()
    task.spawn(function()
        while task.wait(0.3) do
            local myChar = game.Players.LocalPlayer.Character
            local knife = myChar and (myChar:FindFirstChild("Knife") or myChar:FindFirstChildOfClass("Tool"))
            if myChar and knife then
                local closest = nil local dist = 350
                for _, p in pairs(game.Players:GetPlayers()) do
                    if p ~= game.Players.LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        local d = (p.Character.HumanoidRootPart.Position - myChar.HumanoidRootPart.Position).Magnitude
                        if d < dist then dist = d closest = p.Character end
                    end
                end
                if closest then myChar.Humanoid:MoveTo(closest.HumanoidRootPart.Position) if dist < 12 then knife:Activate() end end
            end
        end
    end)
end)

CreateButton("👁️ SMART ESP (RED/GREEN)", 100, function()
    for _, p in pairs(game.Players:GetPlayers()) do
        if p ~= game.Players.LocalPlayer and p.Character then
            local h = p.Character:FindFirstChild("AnasHubESP") or Instance.new("Highlight", p.Character)
            h.Name = "AnasHubESP" 
            h.FillColor = (p.Character:FindFirstChild("Knife") and Color3.fromRGB(255,0,0) or Color3.fromRGB(0,255,100))
        end
    end
end)

CreateButton("🧲 MAGNET ITEMS", 150, function()
    task.spawn(function()
        while task.wait(0.5) do
            for _, item in pairs(workspace:GetDescendants()) do
                if item:IsA("Tool") and item:FindFirstChild("Handle") then
                    item.Handle.CFrame = game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame
                end
            end
        end
    end)
end)

CreateButton("👻 GHOST MODE (NOCLIP)", 200, function()
    game:GetService("RunService").Stepped:Connect(function()
        for _, p in pairs(game.Players.LocalPlayer.Character:GetChildren()) do
            if p:IsA("BasePart") then p.CanCollide = false end
        end
    end)
end)

CreateButton("🌍 JOIN NEAR-START SERVER", 250, function()
    local s = game:GetService("HttpService"):JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?limit=100"))
    if s and s.data then game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, s.data[1].id, game.Players.LocalPlayer) end
end)

print("ANAS HUB V27.5 LOADED SUCCESSFULLY")
